# audit/06 — Residual risk register (Phase 6)

Every unfixed item at MUST-FIX or above, and the material SHOULD-FIX items, with a **compensating control** and an **executable tripwire** and an **owning role**. A risk with neither a compensating control nor a tripwire is not accepted — it is ignored; there are none of those here.

Owning role for all items: **operator (mglaeser)** — the sole maintainer and human-in-command.

| ID | Residual risk | Band | Compensating control (now) | Executable tripwire | Target |
|---|---|---|---|---|---|
| **B-06** | 9 provider credentials were disclosed in chat and are not yet rotated; no vault/rotation. A 10th (`IMESSAGE_API_KEY`) was never disclosed but shares the un-vaulted `.env` and, uniquely, **expires** (90d default) — an expired key is a 401 indistinguishable from a wrong one, and with no fallback transport the digest simply goes quiet. | STOP-SHIP | Repo history is clean (verified); admin key now fails closed on the placeholder; reads expose no secret. The iMessage key is scoped `messages:send` and cannot widen the proxy's admin-scoped recipient allowlist. | Add a startup log-warning + a monthly reminder; alert if `ADMIN_API_KEY` equals the placeholder (already 503s). Record the iMessage key's issue date and alert before the 90-day expiry; alert on repeated digest-send failure so a dead key surfaces as an alarm rather than as silence. | **Rotate all 9 at the providers before serving prod.** Then adopt a host secret store covering all 10. |
| **A-01 / B-35** | CI is rebuilt to be blocking+green-capable, but nothing yet *enforces* it as a required check — an operator could still merge/deploy on red, and the code-author identity can edit `ci.yml`. | STOP-SHIP (esc.) | The rebuilt gate + `deploy.sh` health-check auto-rollback; this audit as the record. | Enable branch protection: require the `test` check, disallow force-push/bypass. A CI job that asserts `ci.yml` is unchanged vs a signed policy hash. | Branch protection owned by the human-in-command; policy file with separate write access. |
| **A-39** | No independent, different-vendor adversarial verifier; author and verifier share a model family. | STOP-SHIP (esc.) | Deterministic golden-fixture gate + (once wired) mutation gate as the non-model arbiter; the partial same-vendor verifier run this engagement. | Mutation score below threshold blocks merge (once `mutmut` is wired). | Add a different-vendor verifier for auth/scoring/deploy changes. |
| **A-02** | The suite's mutation score is unmeasured — its fault-detection power is unknown. | BLOCKER-1 | 171 passing tests + golden fixtures + the red→green evidence in `audit/05`. | `mutmut run` in CI; score `< 0.75` on core logic blocks. | Wire `mutmut` on `app/engine/` + `app/indicators/`. |
| **B-04 / C-03** | No hash-lockfile / dependency-existence gate; CI installs from floors. | BLOCKER-1 | `pip-audit` (blocking) + `detect-secrets` now in CI; every dep verified real this engagement; `lppls` exact-pinned. | `pip-audit` non-zero blocks; add a pre-install existence check. | Generate a hash lockfile; install from it in CI + Containerfile. |
| **A-24 / B-19 / B-31** | No SLO/error-budget; no tested backup/restore of the SQLite DB. | BLOCKER-1/2 | Strong graceful degradation; `/readyz`; `deploy.sh` auto-rollback. | A cron `sqlite3 .backup` + a scheduled restore-drill whose result is a signal; a freshness SLI alert. | Backup+restore drill this week; define an availability SLO. |
| **B-13 / B-24 / B-36** | `LLM_MODEL` is an opaque gateway route; a gateway-side provider/model or failover change can shift judgment/digest text without the serving identity being observable here. | BLOCKER-2 | Exact requested route only (no app substitution) + deterministic/stale degradation; runtime output is a disclaimered note/digest, not a scored decision. Alert code selection remains a dormant future Stage-7/A-B component. | Canary re-eval of a fixed prompt plus gateway telemetry that records the resolved serving model. | Require the gateway to return an attestable resolved model/version, or document the route itself as the governed artifact. |
| **A-13** | 197 mypy errors remain (down from 217; originally recorded as 43); the type-check is no longer advisory but a **ratchet** — the count may fall, never rise (`.github/workflows/ci.yml`, `MYPY_CEILING`). The remaining count is the residual risk. | MUST-FIX | The ratchet blocks any increase, so the debt is capped rather than growing; the step additionally refuses if mypy exits >= 2 or checks fewer than `MYPY_MIN_FILES` source files, so a green ratchet cannot mean an unrun checker. | Lower `MYPY_CEILING` in the same commit that lowers the count; CI fails if the count rises, if mypy cannot run, or if it reads less of the tree than the floor. | Drive to zero. |
| **A-11 / C-02** | The digest sends a `messages:send` bearer key and the digest text to a host that arrives from CONFIGURATION — `IMESSAGE_API_BASE_URL` — where every other outbound host in this service is a literal in code. The residual is the host itself: nothing here bounds WHICH https host a mistyped setting reaches. | BLOCKER-2 (was: unvalidated scheme, now closed) | `app/notify/imessage.py:check_destination()` refuses before a socket opens — https required, plain http only to loopback-equivalent hosts, no path component — so a cleartext send is no longer reachable by typo. The key is scoped `messages:send` and cannot widen the proxy allowlist; `sanitize()` keeps it out of logs and admin responses. `alerts preflight` checks the destination. None of that bounds the host. | `tests/test_imessage_digest.py::TestDestinationSafety` asserts the refusal and that no socket opens. Still needed: the container egress allowlist, which is the only control that bounds a mistyped **https** host. | Egress allowlist with the C-02 staleness check. |
| **A-34 / B-26** | An operator can believe iMessage is configured when it is not. | MUST-FIX (was: silent loss of a working digest, now closed) | `daily_digest_transport` requires `imessage_configured`, not the switch alone, so adding `IMESSAGE_ENABLED=true` to a working SMS deployment no longer flips the transport and kills the digest. The state is reported rather than absorbed: `imessage_enabled_but_unconfigured` on the health projection, a boot warning naming the missing settings, and a failing `alerts preflight` check. | `tests/test_imessage_digest.py::TestTransportSelection` covers enabled-but-blank, half-configured, and enabled-with-SMS-off. Still needed: an alert on N consecutive skipped or failed digests — nothing yet pages when delivery stops. | The consecutive-failure alert is the same tripwire B-26 carries. |
| **C-04 / C-23** | The N/A on C-04 and the low materiality of C-23 both rest on a fact no code in this repository can check: that `IMESSAGE_RECIPIENT`, `SIPGATE_RECIPIENT` and the imessage-proxy instance are all the operator's own. Two transports now mean two places that fact must hold, and a one-line config edit is enough to make a third party the data subject. | STOP-SHIP if breached (C-04's own band) | Recipients are config-pinned and unreachable from a request body or from model output; the proxy's allowlist is admin-scoped so a stolen key cannot add a destination; the proxy's audit store holds no bodies and no recipients. | A test that the digest can address exactly one config-pinned destination per transport (already true, not yet asserted as a control), plus a dated operator attestation re-signed whenever either recipient setting changes. | Attestation now; the single-destination test with the next change to `app/notify/`. |
| **C-05 / C-38** | The LLM note has no factual-grounding gate — it could misstate the snapshot it summarises. | SHOULD-FIX | Disclaimer + "not a probability" framing + `_clean_completion` shape check + science audit; single user. | A deterministic check that the note's stated direction/band is consistent with the snapshot; hallucination-rate monitor. | Add the grounding sanity check. |
| **C-09 / C-36** | AI-generated text is shown to a reader without an explicit "AI-generated" marker (EU AI Act Art. 50 from 2026-08-02). | BLOCKER-2/SHOULD | Disclaimer present; minimal-risk classification recorded (`audit/03` C-09). | Add `ai_generated: true` on the `judgment_call` field + a one-line SMS/UI disclosure. | Ship the marker before 2026-08-02. |
| **B-03 / B-28** | Logs are structured but there are no metrics/traces and detection routes to a human, not an automated response. | MUST-FIX | structlog + `/healthz`/`/readyz` + deploy-time auto-rollback. | A synthetic freshness check wired to restart/rollback. | Add OpenTelemetry traces + golden-signal metrics. |

## Acceptance

The **STOP-SHIP** items (B-06 rotation, A-01/B-35 branch protection, A-39 independent verifier) are **not closed by this engagement** — they require operator/repo-settings actions and a second vendor. They are accepted here only *with* the compensating controls and tripwires above, and they are the lead items in `audit/08`. Per the mandate, the honest status is: **the system should not be treated as independently verified until branch protection is enabled and credentials are rotated.**
