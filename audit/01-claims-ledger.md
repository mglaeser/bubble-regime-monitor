# audit/01 — Claims ledger (Phase 1)

Every claim the system makes about itself, tested against evidence produced in this engagement. A false claim is a finding in its own right (it is what a break-glass responder or acquirer will rely on). Verdicts: **TRUE / FALSE / PARTIAL / STALE**.

| # | Claim | Source | Verdict | Evidence |
|---|---|---|---|---|
| 1 | "transparent, reproducible 0–100 regime heuristic … a reader of the code or of the API alone can reconstruct the entire method." | README:9 | **TRUE** | `app/references.py:REGISTRY` documents every indicator's what/how/why + sources; the golden fixture (`tests/test_golden_fixture.py`) pins the deterministic score (52.43) and seeded MC (median 52.56, seed 20260711) — the method is reconstructable and reproducible. |
| 2 | "**NEVER HTTP 500 ON DATA FAILURE** … Upstream failure must never surface as a 500." | README:17 (epistemic guardrail 5) | **PARTIAL** | Holds for *upstream data* failure: sources degrade / drop-and-renormalize with provenance notes; `GET /score` returns **503** (not 500) when no snapshot exists (`score.py:66`). **But** the claim is silent on *input* validation, and `GET /api/v1/score/history?from=garbage` returns **HTTP 500** (unguarded `datetime.fromisoformat`, `score.py:111`). Verified live via TestClient. → finding **A-25 / A-26**. |
| 3 | "Three framework citations could not be independently verified as of July 2026 … embedded with build-time-verification flags." | README:202, `references.py:70` (`# TODO: verify at build time`) | **STALE (now verifiable)** | All three **resolve to real sources** on search in this engagement: Chen, Chen & Huang (2026) **arXiv:2604.25826** "General-Purpose Technology and Speculative Bubble Detection" (posted 2026-04-28); Basele, Phillips & Shi (2025) **Cowles d2430** "Speculative Bubbles in the Recent AI Boom" (cowles.yale.edu); **BIS Annual Economic Report 2026** (bis.org/publ/arpdf/ar2026e, released 2026-06-28, covers AI-bubble risk). The "unverified" flag and TODO are honest but **out of date**. → finding **C-38 / A-16** (clear the flag; positive result). |
| 4 | "the service must fall back down a defined chain, or drop the indicator and renormalize its block, always attaching a provenance note." | README:17 | **TRUE** | `app/services/compute.py` drop-and-renormalize path + per-indicator provenance; `tests/test_degradation.py`, `tests/test_normalization.py` cover it. LPPLS failure drops D4 and renormalizes Block D (never a placeholder). |
| 5 | "The headline is NOT a probability … structured expert-judgment … not investment advice." | README:9, `score.py`, `DISCLAIMER` on every response | **TRUE** | Disclaimer is attached in `_meta()` of every score response and in the OpenAPI description; epistemic caveats surfaced. Honest self-limitation. |
| 6 | "recomputes twice daily (06:00 / 18:00 UTC)". | README:151 | **TRUE** | `app/scheduler.py` cron triggers. (Docstring in `main.py:55` says 01:00/13:00 for the breadth sweep — a *different* job; not contradictory but under-documented.) |
| 7 | "hard-capped to 160 ASCII characters … if the LLM is unavailable it degrades to a deterministic template, so the digest always sends." | README:170 | **PARTIAL** | Body-generation half is **TRUE**: `app/engine/sms_report.py` deterministic fallback; `sms_max_len=160`; `tests/test_sms_digest.py`, `tests/test_imessage_digest.py`. Delivery half is **narrower than it reads**: "always sends" scopes to LLM unavailability only. Over iMessage there is deliberately no transport fallback (`app/services/digest.py` — a proxy outage drops the digest rather than downgrading to SMS), and a `202` from the proxy means Messages.app accepted the command, explicitly not delivery (imessage-proxy/openapi.yaml, `sendMessage`). Separately, the delivered text is not byte-identical to the sent text: the proxy appends the sending key's `sender_identifier` unless an `admin` key suppresses it, which a `messages:send` key may not do. |
| 8 | "foregrounds a **science audit**: a severity-ranked list of everything currently unclear, incomplete, contested, proxied, judgmental, or deviating from spec." | README:145, `app/services/status.py` | **TRUE** | The status page renders unverified citations, contested GSADF, ETF proxies, the α-range deviation, FRED truncation, stale/dropped indicators, coverage degradation, and live per-source success/failure. This is unusually honest self-disclosure and materially reduces the "confident absence" hazard for *scientific* claims. |
| 9 | CI workflow named `ci` implies changes are tested before landing. | `.github/workflows/ci.yml` | **FALSE (decorative)** | CI is **red on every recent run** and non-blocking (`mypy … \|\| true`; no required status check); production shipped on red. A named gate that does not block is zero gate. → **A-01 / B-01**. |
| 10 | `mypy` type-checking is part of the gate. | `ci.yml:28-29` | **FALSE** | `mypy app \|\| true` — the exit code is discarded. Type errors cannot fail the build. The inline comment ("strict gate tracked; do not silently drop failures") describes an intent that the code contradicts. → **A-13**. |
| 11 | `ADMIN_API_KEY` protects write endpoints. | `security.py:18`, `admin.py` | **TRUE, with a weak default** | `require_admin_key` uses `secrets.compare_digest` (constant-time) and 401s on mismatch — correct. **But** the shipped default is the guessable literal `"change-me-to-a-long-random-string"` (`config.py:34`); if an operator never overrides it, the admin surface is effectively open. → **C-01 / B-06** (fail-closed on the placeholder). |
| 12 | "Reads are public … no credentials/X-API-Key are sent — keep allow_credentials=False." (CORS) | `main.py:94` | **TRUE** | CORS is locked to a single origin, GET-only, `allow_credentials=False`. Consistent with a public read-only API. No over-permissive `*` origin. |
| 13 | `.env.example` documents the real runtime configuration. | `.env.example` | **STALE** | `LPPLS_TIMEOUT_S=600` in the example, but the code default is `1500` (`config.py:66`) and the Atom host needs ≥1500 (600 caused the production LPPLS timeouts). Following the example reproduces a known outage. → **A-32**. |
| 14 | Dependencies are appropriate and available. | `pyproject.toml` | **TRUE (existence), PARTIAL (pinning)** | Every dependency **resolves to a real package** — including the unusual `lppls==0.6.24` (PyPI) and R `exuber` (CRAN). No hallucinated/slopsquat package found. **But** only `lppls` is exact-pinned; everything else is a floor (`>=`), there is **no lockfile / no hash pinning**, and CI installs an *unpinned, hand-edited subset*. → **B-04 / C-03**. |
| 15 | Model is `claude-opus-4-8`. | `config.py:15` | **TRUE, but a floating alias** | The identifier is a family alias with no dated snapshot suffix; the fallback chain (`claude-sonnet-5`, `claude-sonnet-4-6`) is likewise aliased. Provider-side model migration can change behaviour with no code change. → **B-13**. |
| 16 | "EXACTLY ONE TRANSPORT SENDS. If both `IMESSAGE_ENABLED` and `SMS_ENABLED` are true, iMessage wins and sipgate is not called." | README:172, `.env.example`, `config.py:daily_digest_transport` | **TRUE** | `tests/test_imessage_digest.py::TestExactlyOneTransportSends` drives `send_daily_digest` end to end on both transports with call-recording doubles and asserts the other sender is invoked zero times, including on the iMessage-failure path. Mutation-checked: inserting a sipgate fallback fails `test_imessage_failure_does_not_fall_back_to_sipgate`. |
| 17 | "The digest job now names a probable misspelling in its skip reason instead of just going quiet." | README, `.env.example`, `digest.no_transport_reason()` | **TRUE, with a stated reach** | `config.near_miss_env_keys()`; covered by `tests/test_imessage_digest.py::TestMisspeltEnvDetection`. Reach, stated so it is not over-read: the skip reason fires only where NO transport is enabled, and `app/scheduler.py` logs it at boot in the same state. A deployment with `IMESSAG_ENABLED` set **and** `SMS_ENABLED=true` keeps sending over SMS and is never told the iMessage key was dropped — for that case the check is in `alerts preflight`, which is not run automatically. |
| 18 | "`legacy_daily_digest_enabled` reports whether the digest runs on any transport." | `app/alerts/health.py` | **TRUE as now worded** | Was an over-claim: the field is `daily_digest_transport != "none"`, which is true even when the URL/key/recipient are blank — a job that fires daily and skips every time. The comment now says SCHEDULED rather than runs, and `legacy_daily_digest_configured` carries the difference so a half-configured deployment is visible rather than reported as healthy. |

## Claims that describe a human review step

Per the mandate, a documented human-review control in a no-reviewer system is *a claim about a control that does not exist*. **None found.** The project documentation does not claim human code review; it claims automated tests + CI (claims 9–10 above), which are the decorative/red gate. This is, paradoxically, more honest than a false review claim — but it means the operating model's safety rests entirely on a gate that is currently non-functional.
