{
  "schema_version": 2,
  "document_class": "MIDTERM_PANEL_APPROVED_ENGINE_RELEASE",
  "honest_scope": "The exact engine release a human operator approved for the mid-term single-repository panel. The two source commits are PINNED, which is what makes the reviewer independent of the candidate; the artifact digest, the release tag and the identity-document digest are APPROVED, which is what makes it usable by a run that spends money. `engine.provenance_of` returns APPROVED_RELEASE only because all seven binding fields below are present — approving a subset would approve an engine the operator did not fully name. What backs the approval is deterministic reproduction plus an out-of-band human decision, NOT native branch protection, NOT write separation, and NOT third-party attestation.",
  "sha_fields_are_git_commit_ids": "`approved_engine_source_sha` and `approved_engine_protected_sha` are 40-hex git commit ids, not credentials. Every other 64-hex value here is a content digest. An entropy detector cannot tell either apart from a secret, which is the correct default, so they carry audited entries in `.secrets.baseline` — JSON has no comment syntax, so an inline `pragma: allowlist secret` is not available here.",
  "approved_engine_source_sha": "5986c13d661ef94ba6973b164fb7c2fd1bddfb43",
  "approved_engine_protected_sha": "5986c13d661ef94ba6973b164fb7c2fd1bddfb43",
  "approved_engine_artifact_sha256": "cf0b14d2569a5ae99afb9d23d21bf0cdd5408a89eeb671fcab24897163a8b000",
  "approved_engine_release_tag": "midterm-panel-engine-2026-08-14-schema-v2",
  "approved_engine_identity_sha256": "beb82a9fdf5b7c90e20a869dd061c5d894b8e06ecc05037a3eb6eee3af5df8f6",
  "engine_release_binding_sha256": "499c1e22740a24c336b05684a39b95c19402b96de82c2927347a347f7c37313c",
  "binding_digest_honest_scope": "A RESTATEMENT of the seven fields `engine.RELEASE_BINDING_FIELDS` names, as one number, so a human has a single value to compare and a downstream comparison cannot check six fields out of seven. It is not an input: `engine.release_binding` recomputes it from the seven fields, and `releasevalidation` refuses when the recomputation and this value disagree. Storing it without recomputing would be two copies of one claim, which is the drift this lane keeps finding.",
  "identity_document_honest_scope": "sha256 of the `engine-identity.json` asset attached to the approved release. Rebuilding the artifact from the two pinned commits proves the bytes are reproducible, which says nothing about which build produced the approved ones, what ref it ran from, or under which control the operator accepted it. Only the identity document carries that, so a provider-backed run materialises the exact asset, checks it against this digest, and strict-loads it.",
  "the_two_roles_are_the_same_commit_here": "Both role pins are `5986c13d`, which they have not been before, and that is a statement about this repository rather than a shortcut. The two roles exist so the `trustedlane` half can be pinned older than the `verifier` half when only the candidate half moves; they are not required to differ. The invariant that actually protects the lane is `engine.assert_engine_source_is_not_the_reviewed_candidate`, which refuses when EITHER role equals the reviewed head — and it is checked per run against that run's head, not against this document. Pinning both roles at the merged default-branch commit that carries the schema fix keeps the engine a merged, reviewed, operator-named commit while leaving that invariant to do its work.",
  "native_branch_protection": false,
  "control_class": "HUMAN_EXACT_HEAD_COMPENSATING_CONTROL",
  "protection_honest_scope": "`main` has no native branch protection or ruleset on this plan, and no claim of protected-ref provenance is made anywhere in this lane. The compensating control is that a human selects an exact default-branch commit id, approves the exact five identity digests out of band, and the build records `${{ github.ref_protected }}` as the platform reported it — which for build run 31838609804 was `false`. This backs MIDTERM_SINGLE_REPO_* evidence only. The build role key `protected_trusted_lane` is retained for schema compatibility with `trustedlane.enginesource` and does NOT assert that the source ref was protected.",
  "approved_engine_identity_digests": {
    "honest_scope": "A RESTATEMENT of the five digests the build published and the operator approved out of band, so they can be read without downloading the release asset. `releasevalidation` compares every one of them against the materialised identity document and refuses on any difference; nothing reads them as an input.",
    "engine_artifact_sha256": "cf0b14d2569a5ae99afb9d23d21bf0cdd5408a89eeb671fcab24897163a8b000",
    "engine_source_sha256": "c1bd860e29667fb13f62eadb5033f0df547ee8386c0b19c4c2defb16f0472a0a",
    "runtime_lock_sha256": "18ed511e512d4277869206b909f0b9cfbc0485e0241ec0931d463d20babefc2d",
    "sbom_sha256": "2e55ff5093688057dfc5ddbffebcc141bc9ada4371ebff9ac6e8aa7fbc36a471",
    "provenance_sha256": "388b975f4f889ea787b707d3df96d70619ac2eccdb480f9d882c0506714a5acd"
  },
  "approved_engine_build": {
    "honest_scope": "What the platform told the build about itself. A process cannot verify which ref dispatched it or whether that ref was protected; these are recorded as reported, and `build_ref_protected: false` is the reported value rather than an omission.",
    "workflow": ".github/workflows/trusted-engine-build.yml",
    "build_workflow_run_id": 31838609804,
    "build_workflow_run_attempt": 1,
    "build_ref": "refs/heads/main",
    "build_head_sha": "5986c13d661ef94ba6973b164fb7c2fd1bddfb43",
    "build_ref_protected": false,
    "engine_identity_state": "MIDTERM_OPERATOR_APPROVED_ENGINE_IDENTITY",
    "determinism": "the same run rebuilt the artifact from the same two commits and reproduced cf0b14d2569a5ae99afb9d23d21bf0cdd5408a89eeb671fcab24897163a8b000, and a rebuild from the same two commits in an unrelated Linux environment reproduced the same digest and the same 358922 bytes",
    "actions_artifact_id": 9233419540,
    "actions_artifact_zip_sha256": "fa1f32aaf5413bb6f786b98d71cbe3ceb284fed01592653acdf7da9fe943d02f",
    "actions_artifact_note": "The Actions artifact is a ZIP wrapper whose digest is NOT the engine digest, and it expires 30 days after the build. The release below is the durable copy."
  },
  "approved_engine_release": {
    "tag": "midterm-panel-engine-2026-08-14-schema-v2",
    "release_id": 370825800,
    "target_commitish": "5986c13d661ef94ba6973b164fb7c2fd1bddfb43",
    "published_at": "2026-08-14T21:13:02Z",
    "draft": false,
    "prerelease": true,
    "prerelease_note": "GitHub marks this release a prerelease. `releaseasset.resolve_identity_asset` refuses a DRAFT, because a draft can be edited or deleted without changing its tag; a prerelease cannot, so it is recorded rather than refused.",
    "assets": [
      {
        "name": "engine-identity.json",
        "asset_id": 514927290,
        "size_bytes": 5606,
        "sha256": "beb82a9fdf5b7c90e20a869dd061c5d894b8e06ecc05037a3eb6eee3af5df8f6"
      },
      {
        "name": "engine.tar.gz",
        "asset_id": 514927308,
        "size_bytes": 358922,
        "sha256": "cf0b14d2569a5ae99afb9d23d21bf0cdd5408a89eeb671fcab24897163a8b000"
      }
    ]
  },
  "supersedes": {
    "honest_scope": "What this binding replaced, kept so a reader who finds the older digests in run logs or in `.secrets.baseline` history can tell which release they belong to. Nothing reads this; it is not a fallback and there is no path that loads the superseded release.",
    "tag": "midterm-panel-engine-2026-08-10",
    "release_id": 368219671,
    "superseded_reason": "The engine pinned at `c8ba2a72` sent `uniqueItems` inside a `strict` structured-output schema, which the provider rejects with 400 invalid_json_schema at `text.format.schema`, so every count attempt failed before a verdict could be produced. The fix is merged as PR #47 and the engine was rebuilt from the merged commit. The superseded release and its assets are left in place unmodified.",
    "still_published": true
  },
  "operator_action_required": [
    "The engine source deliberately LAGS the candidate. A pull request that changes scripts/verifier is reviewed by the engine pinned here, not by its own edited code; picking up that change means merging it first, rebuilding, re-releasing and re-pinning every field in the binding above.",
    "Re-pinning is a seven-field change, not a one-field change. `engine.release_binding` refuses an incomplete binding, so a partial edit fails closed rather than approving an engine nobody named."
  ]
}
