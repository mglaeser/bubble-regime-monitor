# PROPOSED reviewed-literal allowlist for range A (PR #25).
# NOT operator-approved. Every line below is a literal that secret preflight
# flagged inside the changed content. Preflight is correct to flag them: they
# are credential SHAPES. They are cleared here only because the file whose
# whole purpose is to plant them — tests/test_probe_error_redaction.py —
# proves they never leak. An operator must confirm each line before any real
# provider call is made with this list in force.
#
# Lines beginning "# " are comments.
sk-proj-abcdef123456
sk-proj-DEADBEEFdeadbeef
sk-proj-THIS-IS-THE-REAL-KEY
sk-proj-abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGH
sk-proj-not-even-an-int
